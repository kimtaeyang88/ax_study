[project]
name = "02-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kimtaeyang88", email = "chaosprada@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
]

[project.scripts]
02-python = "02_python:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
